---
title: シミュレーション内ランダム化 — UR10 パレタイジング
---

# シミュレーション内ランダム化 — UR10 パレタイジング

## 学習目標

このチュートリアルを修了すると、以下の内容を習得できます：

- **既存の Isaac Sim シミュレーション**を拡張して、特定のシミュレーションイベントで SDG パイプラインをトリガーする方法
- **アノテータで直接データを取得して手動保存**する方式と、**ライターで暗黙的に保存**する方式の使い分け
- 対象物まわりのライト・マテリアル・テクスチャを**異なるレートで**ランダム化するグラフの構成
- 同一シミュレーション内での**ランダム化／キャプチャグラフの作成と破棄**、**レンダリングモードの動的切り替え**（PathTracing ⇔ RayTracing）

## はじめに

### 前提条件

- [チュートリアル 7: AMR ナビゲーション](07_amr_navigation.md)を完了していること（イベントトリガー型 SDG の発展形です）
- Script Editor からのコード実行に慣れていること

### 所要時間

約 40〜50 分

### 概要

UR10 ロボットがコンベアからビン（箱）を取り、パレットに積み上げる**パレタイジングデモシーン**を土台に、作業中の特定イベントでデータを収集します。裏返ったビンは、ヘルパーオブジェクトで表向きに直してからパレットに積まれます。

データを収集するイベントは 2 つです：

| シナリオ | トリガー | データ収集方式 | レンダリング |
|---|---|---|---|
| **ビン反転（bin flip）** | ビンが反転ヘルパーに置かれたとき | **アノテータから直接取得**して手動でディスク保存 | **PathTracing** |
| **ビン・オン・パレット** | ビンがパレット（または既に積まれたビンの上）に置かれたとき | **BasicWriter** で暗黙的に保存 | 既定（RayTracing） |

収集するアノテータデータは LdrColor（rgb）と instance segmentation です。

!!! note "このチュートリアルの重要な設計方針"
    SDG パイプラインが**実行中のシミュレーションの結果を変えない**こと、キャプチャ後に**加えた変更をクリーンアップする**ことが徹底されています。たとえばマテニアルのランダム化前に元のマテリアルをキャッシュして復元し、作成したグラフとレンダープロダクトはシナリオごとに破棄します。「シミュレーションの副作用なしに SDG を差し込む」パターンとして参考になります。

![パレタイジングの収集データ](https://docs.isaacsim.omniverse.nvidia.com/5.1.0/_images/isaac_tutorial_replicator_palletizing_data.png)

## ステップ 1：実行してみる

このチュートリアルは **Script Editor** から実行します（公式ページにコード全文があります）。スクリプト末尾の `NUM_CAPTURES` を変更するとキャプチャ数を変えられます。スクリプトは UR10 パレタイジングデモシーンをプログラム的に読み込み、開始します。

## ステップ 2：PalletizingSDGDemo クラス

デモは `PalletizingSDGDemo` クラスにまとめられています。主な属性：

| 属性 | 役割 |
|---|---|
| `_bin_counter` / `_num_captures` | 現在のビン番号と要求キャプチャ数の追跡 |
| `_active_bin` | 現在追跡中のビン |
| `_bin_flip_scenario_done` | ビン反転シナリオの完了フラグ（ビンがヘルパーに接触し続けても再トリガーしないため） |
| `_timeline` / `_timeline_sub` | SDG イベントに応じたシミュレーションの一時停止・再開と、状態監視のためのタイムライン購読 |
| `_overlap_extent` | アクティブビン周辺のオーバーラップ照会に使うビンサイズのキャッシュ |
| `_rep_camera` | SDG キャプチャ用の一時的な Replicator カメラ |

**start 関数**は、UR10 デモが読み込まれ動作していることを確認して初期化し、タイムライン購読（`_on_timeline_event`）でシミュレーション状態の監視を開始します。

## ステップ 3：イベント検出とシナリオ分岐

タイムラインが進むたびに `_check_bin_overlaps` がアクティブビンの周囲を監視します。オーバーラップを検出すると `_on_overlap_hit` が呼ばれ、それが 2 つのシナリオのどちらに該当するか判定します。該当すれば、**シミュレーションを一時停止**し、タイムライン購読を解除して、`_run_bin_flip_scenario` または `_run_pallet_scenario` で SDG を開始します。

### ビン反転シナリオ（アノテータ直接方式）

1. レンダリングモードを **PathTracing** に切り替えます。
2. `_create_bin_flip_graph` でランダム化グラフ（カメラ＋ランダム化ライト）を作成します。グラフの生成完了を保証するため、**遅延プレビューコマンド**をディスパッチしてから SDG を開始します。
3. `rep.orchestrator.step_async` を `BIN_FLIP_SCENARIO_FRAMES` 回呼んでグラフを 1 フレームずつ進め、アノテータの `get_data()` でデータを取得してヘルパー関数でディスクに保存します。
4. 終了後、レンダープロダクトとグラフを破棄し、レンダリングモードを **RayTracing に戻して**シミュレーションを再開します。

このシナリオのランダム化グラフでは、ライトの色は定義済みカラーパレットから `rep.distribution.choice(color_palette)` で選択され、カメラは定義済み位置を `rep.distribution.sequence(camera_positions)` で**順番に**移動します。どちらも `rep.trigger.on_frame()` で毎フレーム実行されます。

### ビン・オン・パレットシナリオ（ライター方式）

1. ランダム化グラフがビンとパレットのマテリアル・テクスチャを変更するため、**元のマテリアルをキャッシュ**しておきます。
2. `_create_bin_and_pallet_graph` でグラフ（パレット周りをランダム移動するカメラ、ビンのマテリアル、パレットのテクスチャ）を作成します。
3. **BasicWriter** を使い、`PALLET_SCENARIO_FRAMES` 回の `step_async` でグラフを進めながらライターに書き込ませます。
4. 終了後、レンダープロダクトとグラフを破棄し、**キャッシュしたマテリアルを復元**してシミュレーションを再開します。最後のビンでなければ次のビンをアクティブに設定します。

このシナリオでは**トリガーのレートが 2 段階**に分かれています：

- ビンのマテリアルの色 → **毎フレーム**（`rep.trigger.on_frame()`）
- パレットのテクスチャ（`rep.randomizer.texture`）とカメラ位置（`rep.distribution.uniform`）→ **4 フレームごと**（`rep.trigger.on_frame(interval=4)`）

## まとめ

このチュートリアルでは以下のトピックを扱いました：

1. 既存デモシーンへの**イベントトリガー型 SDG の追加**（オーバーラップ検出→ポーズ→キャプチャ→再開）
2. **アノテータ直接取得**と**ライター**の 2 つのデータ収集方式
3. **異なるレートのトリガー**（毎フレーム／interval=4）によるランダム化
4. グラフ・レンダープロダクトの**動的な作成と破棄**、**レンダリングモードの切り替え**、マテリアルの**キャッシュと復元**によるシミュレーション非干渉の実現

## 次のステップ

- [チュートリアル 9: Cosmos 合成データ生成](09_cosmos.md) - NVIDIA Cosmos 向けのマルチモーダルデータ生成を学びます。
